{
  "dependencies": {
    "dotenv": "^16.0.1",
    "ethers": "^5.7.0",
    "fs": "^0.0.1-security",
    "ganache": "^7.4.1",
    "solc": "0.8.8"
  },
  "scripts": {
    "compile": "yarn solcjs --bin --abi --include-path node_modules/ --base-path . -o . SimpleStorage.sol"
  }
}
